package com.vijay.filepicker.fileProvider

import androidx.core.content.FileProvider

/**
 * This File Provider for this filepicker module so it will not conflict with default one
 */
class FilePickerFileProvider : FileProvider()